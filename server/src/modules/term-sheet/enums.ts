export enum TermSheetStatus {
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  PENDING = 'pending',
}

export enum SafeProgress {
  CREATED = 'created',
  PAID = 'paid',
  AWAITING = 'awaiting',
}
